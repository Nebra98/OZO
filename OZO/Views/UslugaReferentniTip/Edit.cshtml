@model OZO.Models.UslugaReferentniTip

@{
    ViewData["Title"] = "Edit";
}

<h1>Uredi</h1>

<h4>Usluga - Referentni tip</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="IdUsluga" class="control-label">ID - Usluge</label>
                <select asp-for="IdUsluga" class="form-control" asp-items="ViewBag.IdUsluga">
                    <option selected value="">
                        -- Odaberi --
                    </option>
                </select>
                <span asp-validation-for="IdUsluga" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IdReferentniTip" class="control-label">ID - Referentni tip</label>
                <select asp-for="IdReferentniTip" class="form-control" asp-items="ViewBag.IdReferentniTip">
                    <option selected value="">
                        -- Odaberi --
                    </option>
                </select>
                <span asp-validation-for="IdReferentniTip" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Ažuriraj" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Natrag</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
